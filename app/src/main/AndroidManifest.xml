<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.uniovi.appkotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_appkotlin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_appkotlin_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppKotlin">
        <activity android:name=".CorutinasActivity"></activity>
        <activity android:name=".BuclesActivity" />
        <activity android:name=".AddHobbiesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".HobbiesActivity" />
    </application>

</manifest>